[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: marks tests that require a live Postgres connection (deselect with -m "not integration")
    smoke: marks end-to-end smoke tests that require a live WhatsApp/Baileys session
