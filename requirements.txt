pandas>=2.0.0
openpyxl>=3.1.0
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0
python-dotenv>=1.0.0
pytest>=8.0.0
pywin32>=306; sys_platform == "win32"  # Slicers en Excel (solo Windows)
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6  # Para form data en FastAPI
httpx>=0.24.0  # FastAPI TestClient dependency
matplotlib>=3.8.0,<4.0.0  # Graficos cobertura (PNG charts)
python-pptx>=0.6.23,<1.0.0  # Graficos cobertura (PPTX decks)
xlsx2html>=0.6.0,<1.0.0  # Alt image renderer (xlsx -> HTML)
playwright>=1.40.0,<2.0.0  # Alt image renderer (Chrome headless); requires: playwright install chromium
apscheduler>=3.10  # Management UI: daily scheduling
sse-starlette>=1.8  # Management UI: SSE log streaming
sqlglot>=25.0.0,<31.0.0  # SQL AST parser for safety validation (bd_agent)
pyarrow>=15.0,<20.0  # Parquet I/O for sandbox staging (bd_agent)
google-genai>=0.7,<2.0  # Gemini API SDK for bd_agent LLM provider
